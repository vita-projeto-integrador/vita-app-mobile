import { Text, View } from 'react-native';

export default function MapaScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Mapa (em construção)</Text>
    </View>
  );
}